<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content="Guillaume Técher — Personal website" />
    <title>Guillaume Técher</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Young+Serif&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="layout-container">
      <slot />
    </div>
  </body>
</html>

<style is:global>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    --primary-text-color: #1a1a1a;
    --secondary-text-color: #2e2e2e;
    --background-color:  #f3fcff;
    --accent-color: #e8a020;

    --accent-color-about: #5c9cfd;
    --accent-color-blog: #ffe100;
    --accent-color-photograpy: #FF8090;
    --accent-color-projects: #74da58;
    --accent-color-home: #e8a020;

	--background-color-blog: rgb(234, 231, 225);

  }

  html,
  body {
    width: 100%;
    min-height: 100%;
    color: var(--primary-text-color);
	background-color: var(--background-color); ;
    font-family: "Inter", sans-serif;
  }

  .layout-container {
    max-width: 1200px;
    margin: auto;
    padding: 0 80px;
  }

  @media (max-width: 1200px) {
    .layout-container {
      max-width: 1000px;
      margin: auto;
      padding: 0 60px;
    }
  }

  @media (max-width: 600px) {
    .layout-container {
      padding: 0 30px;
    }
  }
</style>
